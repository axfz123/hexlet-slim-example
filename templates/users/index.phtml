<form action="/users" method="get">
  <input type="search" name="s" value="<?= htmlspecialchars($s) ?>" />
  <input type="submit" value="Search" />
</form>
<hr>
<ul>
<?php foreach($users as $user): ?>
    <?php if (str_contains($user['nickname'], $s) || (empty($s))): ?>
    <li><i><?= $user['nickname'] ?></i></li>
    <?php endif; ?>
<?php endforeach; ?>
</ul>
